apiVersion: v1
kind: pod
metadata:
  name: nginx #pod name
  labels: #for grouping purposes
    app: nginx
    tier: front-end
spec:
  containers:
    - name: nginx
      image: nginx #docker hub image name
